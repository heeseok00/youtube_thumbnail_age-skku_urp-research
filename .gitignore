# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── Jupyter ───────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── 가상환경 ──────────────────────────────────────────────────────────────
# conda 환경 yaml은 추적 (ytvenv.yaml 유지)
envs/
venv/
.venv/
ytvenv/

# ── 썸네일 이미지 (용량 큰 원본 이미지) ────────────────────────────────
thumbnails/
Sample_images/

# ── 임베딩 (대용량 바이너리) ──────────────────────────────────────────────
artifacts/embeddings/**/*.npy

# ── HuggingFace 모델 캐시 ─────────────────────────────────────────────────
.cache/
*.bin
*.safetensors
*.pt
*.pth

# ── artifacts 중 재현 가능한 중간 산출물 ──────────────────────────────────
# CSV·JSON 결과는 추적, npy 임베딩만 제외 (위에서 처리)
# artifacts/ 전체를 무시하려면 아래 주석 해제
# artifacts/

# ── 대용량 데이터셋 CSV ───────────────────────────────────────────────────
# YouTube API로 수집한 원본 데이터 (재현하려면 직접 수집 필요)
YT_dataset_v1.csv
# YT_channelsList_v1.csv 는 수동 구성 파일이므로 추적 유지

# ── 문서 파일 ────────────────────────────────────────────────────────────
*.pdf
*.hwp
*.hwpx
*.doc
*.docx
*.ppt
*.pptx

# ── 환경변수 / 시크릿 ─────────────────────────────────────────────────────
.env
*.env

# ── OS / 에디터 ──────────────────────────────────────────────────────────
desktop.ini
Thumbs.db
.DS_Store
*.tmp
~$*

# ── 한글 파일명 원본 (이름 변경 전 구버전) ───────────────────────────────
채널_시각화.ipynb

# ── 기타 임시 파일 ───────────────────────────────────────────────────────
*.log
*.swp
*.bak
